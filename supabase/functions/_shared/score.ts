// Re-exported from @leads/domain — single source of truth (C1: import map).
export {
  SCORE_RULE_VERSION,
  calculateScore,
  temperatureFromScore,
  type ScoreInput,
  type ScoreBreakdown,
} from "@leads/domain/score";
