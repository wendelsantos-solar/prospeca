// Re-exported from @leads/domain — single source of truth (C1: import map).
export {
  CACHE_VERSION,
  roundCoord,
  slug,
  categoryKey,
  placesCacheKey,
  geocodeCacheKey,
  enrichmentCacheKey,
} from "@leads/domain/cache";
