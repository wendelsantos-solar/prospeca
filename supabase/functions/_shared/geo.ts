// Re-exported from @leads/geo — single source of truth (C1: import map).
export {
  EARTH_RADIUS_M,
  haversineMeters,
  haversineKm,
  isValidLatLng,
  boundingBox,
  readPoint,
  toWktPoint,
  type LatLng,
  type BoundingBox,
} from "@leads/geo";
