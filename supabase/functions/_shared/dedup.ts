// Re-exported from @leads/domain — single source of truth (C1: import map).
export {
  MERGE_THRESHOLD,
  REVIEW_THRESHOLD,
  matchConfidence,
  dedupeCandidates,
  type DedupRecord,
  type MatchResult,
  type DedupCluster,
} from "@leads/domain/dedup";
