// Re-exported from @leads/domain — single source of truth (C1: import map).
export {
  scoreInputFromPlace,
  scoreInputFromRow,
  type PlaceLike,
  type PlaceRow,
} from "@leads/domain/score-input";
