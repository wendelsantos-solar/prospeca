# ── Frontend (bundled — public values only, never secrets) ──────────
# NOTE (monorepo): env files live at the REPO ROOT; apps/web reads them via
# vite `envDir`. Do not move them into apps/web.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Browser key: restrict by domain; enable only Maps JavaScript API.
# Optional — leave empty to use the OSM/Leaflet map (see MAP_* below).
VITE_GOOGLE_MAPS_BROWSER_KEY=
# Data mode: "real" (Supabase) or "demo" (local mock data).
# In production this MUST be "real". Missing real config => config error
# screen, never a silent mock fallback.
VITE_DATA_MODE=demo

# ── Map (OSM/Leaflet — no Google required) ──────────────────────────
# Tile URL + attribution are configurable; never hardcode them in components.
# Attribution is MANDATORY per the tile provider's terms.
VITE_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
VITE_MAP_ATTRIBUTION=© OpenStreetMap contributors
VITE_USE_OSM_MAP_PROVIDER=true

# ── Edge Functions (Supabase secrets — set via `supabase secrets set`) ─
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
APP_URL=
APP_ENV=development

# Google (OPTIONAL — system works fully without these keys). Restrict by API.
GOOGLE_MAPS_SERVER_KEY=

# ── Provider selection flags (Strangler; default off = Google) ──────
USE_OSM_GEOCODER=false
USE_OSM_PLACES=false
USE_ASYNC_ENRICHMENT=false

# ── Geocoding (Nominatim / OSM) ─────────────────────────────────────
GEOCODER_BASE_URL=https://nominatim.openstreetmap.org
GEOCODER_USER_AGENT=leads-platform/1.0 (contact@example.com)
GEOCODER_TIMEOUT_MS=10000

# ── Places discovery (Overpass / OSM) ───────────────────────────────
OVERPASS_BASE_URL=https://overpass-api.de/api/interpreter
OVERPASS_USER_AGENT=leads-platform/1.0 (contact@example.com)
OVERPASS_TIMEOUT_MS=30000
OVERPASS_CONCURRENCY=2

# ── Provider resilience ─────────────────────────────────────────────
PROVIDER_MAX_RETRIES=3
PROVIDER_BACKOFF_MS=1000
ENRICHMENT_CONCURRENCY=3
ENRICHMENT_TIMEOUT_MS=15000

# ── Optional enrichment provider ─────────────────────────────────────
ENRICHMENT_PROVIDER=null
ENRICHMENT_API_KEY=
